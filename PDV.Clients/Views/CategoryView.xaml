
<ui:FluentWindow
    x:Class="PDV.Clients.Views.CategoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Categorias"
    Width="1200"
    Height="750"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,12,0"
                    Appearance="Secondary"
                    Command="{Binding BackCommand}"
                    Icon="{ui:SymbolIcon ArrowLeft24}"
                    ToolTip="Voltar para Dashboard" />
                <ui:SymbolIcon
                    Margin="0,0,12,0"
                    FontSize="28"
                    Symbol="Production24" />
                <TextBlock
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="Categorias" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,8,0"
                    Appearance="Secondary"
                    Command="{Binding RefreshCommand}"
                    Icon="{ui:SymbolIcon ArrowSync24}">
                    <TextBlock Text="Atualizar" />
                </ui:Button>

                <ui:Button
                    Appearance="Primary"
                    Command="{Binding NewCategoryCommand}"
                    Icon="{ui:SymbolIcon Add24}">
                    <TextBlock Text="Nova Categoria" />
                </ui:Button>
            </StackPanel>
        </Grid>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,8"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Foreground="#FF5555"
            Text="{Binding ErrorMessage}"
            TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <ui:Card
                Grid.Column="0"
                Padding="0"
                VerticalAlignment="Stretch"
                VerticalContentAlignment="Stretch">
                <ui:DataGrid
                    AutoGenerateColumns="False"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    ItemsSource="{Binding Categories}"
                    SelectedItem="{Binding SelectedListItem, Mode=TwoWay}"
                    SelectionMode="Single"
                    VerticalScrollBarVisibility="Auto">
                    <ui:DataGrid.Columns>
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Name}"
                            Header="Nome" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Description}"
                            Header="Descricao" />
                    </ui:DataGrid.Columns>
                </ui:DataGrid>
            </ui:Card>

            <ScrollViewer Grid.Column="2">
                <ui:Card
                    Padding="24"
                    VerticalAlignment="Stretch"
                    VerticalContentAlignment="Stretch">

                    <Grid>
                        <Grid DataContext="{Binding SelectedCategory}">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Margin="0,0,0,16"
                                FontSize="18"
                                FontWeight="SemiBold"
                                Text="Detalhes da Categoria" />

                            <StackPanel Grid.Row="1">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Nome *" />
                                <ui:TextBox
                                    Margin="0,0,0,12"
                                    PlaceholderText="Nome da categoria"
                                    Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBlock
                                    Margin="0,0,0,6"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Descricao *" />
                                <ui:TextBox
                                    Margin="0,0,0,12"
                                    PlaceholderText="Descricao da categoria"
                                    Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                </Grid>
                            </StackPanel>

                            <StackPanel
                                Grid.Row="2"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal">
                                <ui:Button
                                    Margin="0,0,8,0"
                                    Appearance="Secondary"
                                    Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                    Content="Excluir"
                                    Icon="{ui:SymbolIcon Delete24}" />

                                <ui:Button
                                    Appearance="Primary"
                                    Command="{Binding DataContext.SaveCategoryCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                    Content="Salvar"
                                    Icon="{ui:SymbolIcon Save24}" />
                            </StackPanel>
                        </Grid>

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="16"
                            Foreground="Gray"
                            Text="Selecione uma categoria ou crie uma novo.">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedCategory}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </ui:Card>
            </ScrollViewer>
        </Grid>
    </Grid>
</ui:FluentWindow>